{
  "name": "music-hub",
  "private": true,
  "scripts": {
    "build": "bash scripts/build-cloudflare-pages.sh",
    "build:site": "npm --prefix site ci && npm --prefix site run build",
    "dev": "npm --prefix site run dev",
    "preview": "npm --prefix site run preview"
  }
}
